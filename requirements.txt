# Core dependencies from the user
livekit-agents[silero,turn-detector]~=1.2
livekit-plugins-noise-cancellation~=0.2
python-dotenv

# Note: Additional models are downloaded via
# python src/agent.py download-files